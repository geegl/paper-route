# 部署指南

游戏是**纯静态网页**（无后端、无数据库），部署 = 把文件放到任何一个能提供 HTTP 服务的地方。

## 方式一：局域网游玩（已就绪，零配置）

本机服务器已监听全部网络接口，同一 Wi-Fi 下的任何设备（手机 / 平板 / 电脑）直接访问：

```
http://192.168.31.168:8137/
```

- 手机 Safari / Chrome 直接打开即可玩，触屏按钮会自动出现
- 服务器随本机关机而停止；重新双击 `开始游戏.command` 即可恢复
- IP 地址若变化（路由器重新分配），用 `ipconfig getifaddr en0` 查询新地址

## 方式二：GitHub Pages（免费公网，约 3 分钟，无需装任何工具）

1. 登录 [github.com](https://github.com) → 右上角 **+** → **New repository**
2. 仓库名填 `paper-route`，选 **Public**，点 **Create repository**
3. 在新仓库页面点 **uploading an existing file**，把 `送报少年-部署包.zip` 里的 `paper-route` 文件夹**整个拖进去**（包含 index.html、css、js、lib、.nojekyll），点 **Commit changes**
4. 仓库 **Settings → Pages** → Branch 选 `main` + `/ (root)` → **Save**
5. 等 1–2 分钟，访问 `https://<你的用户名>.github.io/paper-route/`

> 这个地址全网可访问，手机也能开，且自动支持 HTTPS。

## 方式三：Netlify Drop（免费公网，拖一下就好）

1. 打开 [app.netlify.com/drop](https://app.netlify.com/drop)（需免费注册）
2. 把解压后的 `paper-route` 文件夹拖进页面
3. 立即得到一个 `xxx.netlify.app` 的公网地址

## 需要我代劳公网部署？

告诉我选哪条路：

- **GitHub 路线**：批准我安装 `gh` 命令行（`brew install gh`），你在浏览器里完成一次 `gh auth login` 授权，之后建仓库、推送、开启 Pages 全部由我完成
- **其他托管商**（Netlify / Cloudflare Pages / Vercel）：同样需要安装其 CLI 并完成一次账号授权

（公网发布涉及你的账号与对外内容，所以工具安装和授权这两步需要你点头或亲手操作。）

## 打包内容说明

`送报少年-部署包.zip` 内是游戏的完整运行文件（index.html / css / js / lib / .nojekyll / README），不包含开发脚本与审计截图。`.nojekyll` 用于 GitHub Pages 跳过 Jekyll 处理。
